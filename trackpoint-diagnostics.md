# 指点杆（PS/2 TrackPoint）链路诊断手册

指点杆行为异常时用这套方法定位。核心思路是**把不可见的 PS/2 链路变成可读的数字**，用一次采样同时证实一个假设、排除多个假设，避免凭直觉猜。

配套文件：`trackpoint-diagnostics.patch`（一键装回诊断代码）。

> 平时工程里**不保留**诊断代码。需要时打补丁，查完再撤掉。

---

## 什么时候用

指点杆出现下列任一症状：

- 指针乱跳、乱飞，方向没规律
- 轻推没反应，用力推才动（或者反过来，一碰就飞）
- 某个方向完全不动
- 手不碰指点杆时指针自己漂
- 完全没输出

---

## 一、装上诊断代码

```bash
cd keyboards/yj233_split67
patch -p1 < trackpoint-diagnostics.patch
```

补丁改三个文件：

| 文件 | 加了什么 |
| --- | --- |
| `pointing.c` | 帧计数器、载荷极值统计、最近 8 个原始包的环形缓冲、三个输出函数 |
| `keymaps/vial/keymap.c` | 三个自定义键码 `TP_STAT` / `TP_ZERO` / `TP_RAW` 及 `process_record_user` |
| `keymaps/vial/vial.json` | 对应的 `customKeycodes`，让这三个键在 Vial GUI 里可选 |

如果 `pointing.c` 后来改动过导致补丁打不上，看本文末尾「补丁打不上时」。

## 二、编译烧录

**只需烧带指点杆的那半边。**

WSL2 下用 `flash_wsl.sh`（`qmk flash` 在 WSL2 下会卡死，原因见 `readme.md` 的烧录章节）：

```bash
cd /home/yj/yj-private/vial-qmk
./keyboards/yj233_split67/flash_wsl.sh left    # 或 right，看指点杆在哪半
```

原生 Linux / macOS：

```bash
qmk flash -kb yj233_split67 -km vial -bl uf2-split-left    # 或 uf2-split-right
```

⚠ 必须带左右 target（`EE_HANDS`），别用不带 `-bl` 的普通烧录 —— 详见 `readme.md`。

> **指点杆乱跳到没法操作电脑时**：先用应急开关把这半的拓展模块关掉（按住该半的 `Delete`/`Backspace` → 单击 RESET → 看到红闪松手），操作完再正常重启恢复。详见 `readme.md` 的「开机应急：临时禁用某一半的拓展模块」。注意采样期间必须让模块处于启用状态，所以采样前要正常重启一次。

## 三、在 Vial 里绑键

烧录后 Vial 会自动读到新的 `customKeycodes`。在键码选择器里找到并绑到三个空位（Layer 1 上的 `_______` 就行，用完改回来）：

| Vial 里显示 | 名字 | 作用 |
| --- | --- | --- |
| Custom 0 | `TPSTAT` | 把统计数字**当键盘输入敲出来** |
| Custom 1 | `TPZERO` | 计数器清零 |
| Custom 2 | `TPRAW` | 敲出最近 8 个原始包 |

> ⚠ **诊断键只在 USB master 上生效**（`process_record_user` 跑在 master）。采样时**只插带指点杆的那半边**，另一半拔掉。否则读到的是没有指点杆那半边的计数器（会显示 `NONE` 且全是 0）。

---

## 四、跑三组测试

打开记事本或任意文本框（结果是"打字"出来的，需要有输入焦点）。

### A 组 · 完全不碰（最关键）

1. 按 `TP_ZERO`
2. **手完全离开指点杆，静置 10 秒**
3. 按 `TP_STAT`

静止时 PS/2 stream mode **不应该发任何包**。这一组直接判定模块是不是在自说自话。

### B 组 · 轻推

1. 按 `TP_ZERO`
2. 用平常力度朝**一个明确方向**（比如只朝右）推几次
3. 按 `TP_STAT`，再按 `TP_RAW`

看该方向的轴有没有响应、数值合不合理。

### C 组 · 用力推

1. 按 `TP_ZERO`
2. 用力推一次（复现"飞走"的那种力度）
3. 按 `TP_STAT`，再按 `TP_RAW`

看极值有没有顶到饱和、`brst` 是不是大于 1。

---

## 五、读数

### `TP_STAT` 输出

```
TP ok=1050 err=0 rsy=0 ovf=0 stl=0 pkt=350 x=+0..+135 y=-106..+95 btn=00 brst=1
```

| 字段 | 含义 |
| --- | --- |
| `TP` / `PAD` / `NONE` | 开机探测到的模块：TrackPoint / TPS43 触摸板 / 没有 |
| `ok` | 通过 start/parity/stop 校验的帧数 |
| `err` | **被拒绝的帧数 —— 物理层健康度** |
| `rsy` | 找包边界时丢弃的字节数 —— **相位健康度** |
| `ovf` | 包头的 X/Y 溢出位被置位、因而丢弃的包 |
| `stl` | 残包超时丢弃次数 |
| `pkt` | 成功解出的完整包数 |
| `x=` `y=` | 原始每包 delta 的**最小..最大值**（未经 `TP_SPEED_MULT` 放大） |
| `btn` | 期间见过的所有按钮位的 OR（十六进制） |
| `brst` | **单次轮询里最多累加了几个包** |

### 黄金判据

> **`ok` 应该正好等于 `pkt × 3`**

PS/2 鼠标包是 3 字节。这个等式成立，就说明**一个字节都没丢、相位从头到尾没错过**。它是把「链路问题」和「内容问题」一刀切开的判据。

### 判读表

先看链路，链路干净了再看内容。

| 现象 | 结论 | 方向 |
| --- | --- | --- |
| `err` 相对 `ok` 明显偏大 | 物理层在丢帧 | 查供电、CLK/DATA 上拉、焊点、FPC |
| `err`≈0 但 `rsy` 大 | 字节完好、包相位错乱 | 固件解包逻辑问题 |
| `ok`≈0 | 模块完全静默 | 没供电，或复位后 reporting 没被重新打开 |
| `ok == pkt×3` 且 `err`=`rsy`=0 | **链路完美，问题在内容** | 往下看 |
| A 组 `pkt` > 0 | 模块无输入时自发报告运动 | 模块内部故障或漂移补偿失效 |
| A 组 `btn` ≠ `00` | 按钮位卡住，移动会变成拖拽 | 查按钮线路 |
| 某个轴恒为 `+0..+0` | **那个轴的信号通路断了** | 模块硬件故障 |
| 某轴顶在 ±255 附近 | 该轴饱和 | 模块故障或灵敏度过高 |
| `brst` > 1 | 多个包被累加后再乘 `TP_SPEED_MULT`，放大成一次大跳 | 轮询被拖慢，查主循环耗时 |

### `TP_RAW` 输出

```
f28 x+0 y-5 | f28 x+0 y-3 | f08 x+0 y+5 | ...
```

`f` 是包首字节（byte0）的十六进制：

| 位 | 含义 |
| --- | --- |
| bit3 | 恒为 1（PS/2 鼠标包的固定标志） |
| bit0/1/2 | 左键 / 右键 / 中键 |
| bit4 / bit5 | X / Y 的符号位 |
| bit6 / bit7 | X / Y 溢出位 |

所以 `f08` = 无按键、XY 都是正值；`f28` = bit5 置位，Y 是负值。

`x` / `y` 是**符号扩展后的原始 delta**，还没乘 `TP_SPEED_MULT`。

---

## 六、查完之后撤掉

```bash
cd keyboards/yj233_split67
patch -p1 -R < trackpoint-diagnostics.patch
```

`-R` 是反向应用。撤掉后按上面第二节重新编译烧录即可。

**注意：撤掉的只是诊断代码。** `pointing.c` 里 `ps2_error` 的检查、包首字节的严格判据、残包超时这三项属于**解包逻辑修复**，不在补丁范围内，会一直留在工程里。

---

## 案例：2026-08-27，X 轴间歇性断路

留作范例，重点是**哪些假设被数据推翻了** —— 下次别再走同样的弯路。

### 症状

1. 一开始正常，某一刻指针猛地飞到屏幕最左侧，之后指点杆完全无输出（外接鼠标正常）
2. 之后变成：轻推基本不动，用力推则指针瞬间飞走，方向不固定，指针乱跳
3. **重启键盘不恢复**
4. **只插带指点杆的那半边，症状照旧**（排除了 split 转发路径）

### 最初的假设（后来全被推翻）

当时从代码推断出一条看起来很完美的链条：PIO 驱动在帧校验失败时返回 `0x00` 且只置 `ps2_error`，而解包代码从不检查它 —— `0x00` 落在包首位置会被 `b & 0x08` 判据静默丢掉，**丢一个字节 → 包相位永久右移**，于是 X 字节被当成 flags，符号位随机，用力推（大 delta）就乱飞、轻推（小 delta）就不动。再往上推一层，怀疑物理层劣化（3.3V 供电不足、CLK/DATA 上拉失效、焊点松动）是丢帧的源头。

**第一次采样直接推翻了整条链**：

```
TP ok=1050 err=0 rsy=0 ovf=0 stl=0 pkt=350
```

`1050 = 350 × 3` 精确整除，`err=0`、`rsy=0`。链路完美，一个字节没丢过，相位从来没错过。物理层和失步假设同时出局 —— 也顺带证明了 **3.3V 供电对数字通讯完全够用**。

教训：**链路健康度必须先测再推断。** 从代码缺陷推导出的因果链再自洽，也可能整条都不成立。

### 第二轮：改测载荷

加了极值统计和原始包记录后：

| 测试 | pkt | x 范围 | y 范围 | btn | brst |
| --- | --- | --- | --- | --- | --- |
| A 完全不碰 | 34 | **+0..+0** | -8..+13 | 00 | 1 |
| B 朝右推 | 32 | **+0..+0** | -28..+13 | 00 | 1 |
| C 用力推 | 17 | **+0..+135** | -106..+95 | 00 | 1 |

两组 `TP_RAW` 共 16 个包，**每一个的 x 都是 `+0`**。

### 结论

**X 轴信号通路间歇性断路，模块内部硬件故障。**

- B 组明确朝右推，32 个包里 X 极值是 `+0..+0` —— 一次都没非零。同批包的 Y 正常变化，说明模块在正常采样发包
- C 组用力推时 X 突然蹦出 `+135`，`135 × TP_SPEED_MULT(3) = 405` 像素 —— 就是"一下飞走"
- 「平时恒 0、用力偶发巨大跳变」是机械性断裂的特征：静置时断开，形变把裂缝压合一瞬间。电子器件击穿会给出**恒定**错误值，不是间歇性的
- TrackPoint 模块只引出 VCC/GND/CLK/DATA(+RST)，X 轴的应变片和 ADC 全在模块内部；`err=0` 已证明外部走线完好 → 故障必在模块内部

同批数据顺带排除了另外三个候选：`btn=00` 全程排除按钮卡住（乱跳不是拖拽）；`brst=1` 全程排除多包累加放大；Y 轴响应正常、且每次上电都会跑 `trackpoint_reset()` + `0xFF` 完整复位而症状依旧，排除灵敏度/校准配置问题。

另外 A 组手完全离开时仍有 34 个包、Y 在 ±13 波动 —— 静止本该完全不发包，说明 Y 轴也在劣化，不只是 X 坏了。

### 处理

换模块。换上后原样跑一遍 A/B/C 验收：X 应该有正常范围的值，A 组 `pkt` 应接近 0。

换之前先排查是什么弄坏的，否则新模块会重蹈覆辙：

1. **杆是否被顶住** —— 装好上盖/键帽后杆应能自由回中、四周有间隙。持续的侧向预压会让应变片焊点加速疲劳
2. **安装应力** —— 螺丝过紧、垫片不平、FPC 被折压
3. **带电插拔** —— 信号脚先于 GND 接通时电流会经 ESD 二极管倒灌。以后一律断电插拔

---

## 补丁打不上时

`pointing.c` 若已改动导致补丁失败，手工加回四处即可：

1. **计数结构与原始包环形缓冲** —— 放在 `trackpoint_get_report()` 之前
2. **解包循环里的埋点** —— `ps2_error != PS2_ERR_NONE` 时 `err++`；包首判据失败时 `resync++` / `ovf++`；成功解包后更新 `pkts`、x/y 极值、`btnseen`，并写入环形缓冲；循环结束后更新 `maxburst`
3. **三个输出函数** —— `tp_stats_send()` / `tp_raw_send()` / `tp_stats_reset()`，用 `send_string()` 输出
4. **keymap.c 与 vial.json** —— 三个 `QK_KB_n` 键码 + `process_record_user` 分发 + `customKeycodes` 声明

补丁文件本身就是最完整的参考，直接读 `trackpoint-diagnostics.patch` 照抄即可。

### 为什么用 `send_string` 而不是 console

`CONSOLE_ENABLE` 要额外的 USB 端点和 `qmk console` 工具。`send_string` 把结果当键盘输入敲出来，任何文本框都能收，零依赖 —— 代价是采样时必须保持文本框有输入焦点。
